[project]
name = "jobhunter"
version = "0.1.0"
description = "Backend agent that screens job boards (LinkedIn, Xing, StepStone, Indeed, Glassdoor) for matching postings."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
