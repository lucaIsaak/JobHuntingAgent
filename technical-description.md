There are multiple ways through API's to collect job data. 

Currently used: 
- Greenhouse API, uses the company name and can query all open jobs in a JSON
- "JobsPipe" uses greenhouse but already does some of the data cleaning. https://jobspipe.dev/blog/greenhouse-api-jobs - We can potentially look into this to see if it helps. 

Other sources are mentioned in the scrapers 